export default function Span() {
  return (
    <div className="info-span">
      <p className="info-span-desktop">
        Click on an object from the scene to discover it
      </p>
      <div className="info-span-mobile">
        <p>
          The site works better on desktop screen sizes. Click on an object from
          the scene to discover it
        </p>
      </div>
    </div>
  );
}
